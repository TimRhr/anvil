# Anvil — fail2ban-Action: Ban/Unban via Gotify melden
[Definition]
actionstart =
actionstop =
actioncheck =
actionban = /usr/local/bin/anvil-notify --priority 6 --title "fail2ban: IP gebannt" "Jail <name>: <ip> wurde gebannt (Treffer: <failures>)."
actionunban = /usr/local/bin/anvil-notify --priority 3 --title "fail2ban: Ban aufgehoben" "Jail <name>: <ip> ist wieder freigegeben."

[Init]
name = default
