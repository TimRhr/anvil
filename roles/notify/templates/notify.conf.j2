# {{ ansible_managed }}
# Anvil-Benachrichtigungs-Konfiguration (ntfy). Mode 0600 — enthält Token.
NTFY_SERVER="{{ notify_ntfy_server }}"
NTFY_TOPIC="{{ notify_ntfy_topic }}"
NTFY_TOKEN="{{ notify_ntfy_token }}"
PRIORITY_DEFAULT="{{ notify_priority_info }}"
APP_NAME="{{ notify_app_name }}"
SPOOL_DIR="{{ notify_spool_dir }}"
LOG_FILE="{{ anvil_log_dir }}/notify.log"
