# Anvil-Inventory — bewusst minimal.
# Anvil härtet immer den lokalen Host (Pull-Modell). Das Playbook nutzt
# connection: local; ein Remote-Inventory ist nicht vorgesehen.
[anvil_targets]
localhost ansible_connection=local
